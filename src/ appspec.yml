version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/code_test
permissions:
  - object: /home/ubuntu/code_test/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu